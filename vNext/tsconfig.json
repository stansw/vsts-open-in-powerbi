{
  "compilerOptions": {
    "module": "amd",
    "moduleResolution": "node",
    // Target "es5" for compatibility, but use es2015.promise library which gets downleveled 
    // by the TypeScript compiler.
    // https://github.com/Microsoft/TypeScript/wiki/What%27s-new-in-TypeScript#downlevel-async-functions
    "target": "es5",
    "lib": ["dom", "es2015.promise", "es5"],
    "rootDir": ".",
    "outDir": "out",
    "types": [
      "vss-web-extension-sdk",
      "mocha"
    ]
  },
  "include": [
    "src/**/*.ts",
    "test/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
